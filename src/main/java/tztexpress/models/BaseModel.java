package tztexpress.models;

/**
 * BaseModel class, for correct handling of Generic Results - DEPRECATED, will be removed.
 */
public abstract class BaseModel {
    // Note: keep as clean as possible.
}
